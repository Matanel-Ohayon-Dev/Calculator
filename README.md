# Calculator
C++ arithmetic expression calculator using recursive descent parsing. Supports +, -, *, / with correct operator precedence and nested parentheses. Tokenizes input into operands and operators, then evaluates via an expression → term → factor grammar. Includes input validation, division-by-zero protection, and clean memory management.
